spring:
  datasource:
    url: jdbc:postgresql://localhost:54327/sistema-transicion
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
    hikari:
      minimumIdle: 1
      maximumPoolSize: 5
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate.ddl-auto: create
    
eureka:
  client:
    enabled: false
      
server:
  port: 8462